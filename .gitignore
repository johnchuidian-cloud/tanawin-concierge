# Secrets never enter this repo (the mirror is public). Ops scripts read
# credentials from ../tanawin-menu/.env.local (the shared Supabase project).
.env
.env.*
staff-login.txt

# Local scratch (also excluded from deploys because deploys build from git).
tmp/
node_modules/
.DS_Store
